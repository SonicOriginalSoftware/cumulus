import { long_company_name } from "$lib/index.js"

// export const prerender = true

export function load() {
  return {
    section: `Sonic Original Software`,
    title: `Contact`,
    description: `Get in touch with ${long_company_name}`
  }
}
