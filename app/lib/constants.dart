import 'package:flutter/material.dart';

const double drawerIconSize = 56;
const double iconSize = 24;
const double actionButtonPadding = (drawerIconSize - iconSize) / 2;
const EdgeInsets appBarButtonPadding = EdgeInsets.only(
  left: actionButtonPadding,
  right: actionButtonPadding,
  top: actionButtonPadding + 8,
  bottom: actionButtonPadding + 8,
);
